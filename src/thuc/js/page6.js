const WISHLIST_ITEMS = [
  {
    "id": "item-1",
    "category": "deal boxset",
    "badge": "🔥 Giảm 15%",
    "badgeClass": "deal",
    "condition": "Độ mới: 98% (Kèm màng co)",
    "image": "https://lh3.googleusercontent.com/aida-public/AB6AXuBAsTI3CYI3tgmKzTZRBl3tMSwe1eNFaMETpI9ccIffgMqKMGFV2YuZrDNhDbVCa6B65htH_XxUKqhv0BkvJF2u_0QjxjASv_WMLmHN6Kwf1751TZ1SdQTvz_OgoqDv5z5SuGXzqk-hLT6ZLw0cnWF5XdFbXEGBU1e_mHwxJoou0twaeF-omaCw0aenhpt7T-Frm6n68qRpp2_JfTue86A0Tz2eGyIIbWYEGs2aSvSvlMjOADwOFKXApQ",
    "status": "3 người đang bán",
    "place": "TP. HCM, HN",
    "meta": "Tập 1 - 11 Trọn bộ • NXB Trẻ",
    "title": "Chainsaw Man Boxset (Full 11 Tập)",
    "sub": "Tác giả: Tatsuki Fujimoto",
    "price": "450.000đ",
    "old": "530.000đ",
    "actions": [
      [
        "🛒",
        "Mua ngay",
        "primary"
      ],
      [
        "🏪",
        "Xem 3 người bán",
        "secondary"
      ]
    ]
  },
  {
    "id": "item-2",
    "category": "rare",
    "badge": "💎 Hàng hiếm sưu tầm",
    "badgeClass": "rare",
    "condition": "Bản Kanzenban 9 Tập",
    "image": "https://lh3.googleusercontent.com/aida-public/AB6AXuAklsOpGSQkCLiJ-E-6SWUGEYkTT_jK1DvgNOTRTWNNGMjarm9aR1sWnWx2UkneqRi7cQbYnT88jMlQwFbTGQPZGqhpACmn9syqeMGDb9hqXFbzhqsyS2FgQq6XH5taw_HYha0fuNJasUmIsGHM-yDPVKA1E2SvpazFIDn8AcOANA6_uw6z3S5xqFgwBvw9grwduSn8yKXfd37Jk8sAPsukwxF1Xtg84pdL6I5wl8BO7mCZMYIX_hxKyg",
    "status": "Có 1 tin bán mới hôm nay",
    "place": "Hà Nội",
    "meta": "Trọn bộ 1-9 (Bìa rời) • NXB Kim Đồng",
    "title": "Monster Kanzenban Naoki Urasawa",
    "sub": "Người bán: QuangHuy_Collector (★ 5.0)",
    "price": "1.200.000đ",
    "old": "",
    "note": "(Bao gồm phí bảo hiểm Escrow)",
    "actions": [
      [
        "💬",
        "Liên hệ người bán",
        "dark"
      ],
      [
        "👁",
        "Chi tiết ảnh chụp",
        "secondary"
      ]
    ]
  },
  {
    "id": "item-3",
    "category": "boxset",
    "badge": "Có sẵn 5 bộ",
    "badgeClass": "neutral",
    "condition": "Bản bìa gập cao cấp",
    "image": "https://lh3.googleusercontent.com/aida-public/AB6AXuA1FVb77L_0IjoIYGT4eHtxqxgZOmDxI7g3iIB_GL7S0EfQUMAHwqOfmj2bZPIv4KYZMmQn8a5Sy2vSr6By8VGTcSF23LNmbcJ6kbBm1zkXa9ScQ1YIdxeMyd4yk6sxhUn8pWCzVgFFggYWtdCkcJwrKPAmpwRkyvAzSUDv9hAoFIMFwPc9uunX4GNAJaixqmXKQkKJhGjMdct4W8-iZLGgtk3lGfWFoUAeTHvsiBjXRVxuQJaWzeol_A",
    "status": "Giá từ 550.000đ",
    "place": "Có hỗ trợ ship COD",
    "meta": "Full 24 Tập Truyện Dài • Fujiko F. Fujio",
    "title": "Doraemon Truyện Dài Bản Kỷ Niệm 50 Năm",
    "sub": "Tình trạng sách: Mới 99% nguyên seal",
    "price": "550.000đ - 620.000đ",
    "old": "",
    "actions": [
      [
        "📋",
        "So sánh 5 shop",
        "primary"
      ],
      [
        "🛒",
        "Thêm giỏ",
        "secondary"
      ]
    ]
  },
  {
    "id": "item-4",
    "category": "rare",
    "badge": "⏸ Tạm hết người bán",
    "badgeClass": "soldout",
    "condition": "Bản Nhật Obi zin",
    "image": "https://lh3.googleusercontent.com/aida-public/AB6AXuBWgiMVtlfsC3FXQE-hXeykzl_WE25rKXfr09WT-minoHQxyF5RgWzacHkjnOxoDJ3n8vxlg_Q_ie9GczKx4M-fKkyeuT76AFJ9T1OWk-CrrbYuydqfhj31Fs3OA1dvm8yaMauKTweM3pbLq9czygDtds42gj8D9uZD09VYG4E3Lhe-W228PGmZM55Fp2HjXjLKRTaa3uiX2FhJMrMOXGelN2x3XsRLqssJsqdHmKHM-OEHM6oeUCXhyg",
    "status": "Radar đang chờ quét",
    "place": "Giá mong muốn: ~280k",
    "meta": "Tập 37 (Cực hiếm) • Takehiko Inoue",
    "title": "Vagabond Tập 37 (Bản Nhật Obi Zin)",
    "sub": "Hiện chưa có bài đăng bán phù hợp",
    "price": "",
    "old": "",
    "note": "⚡ Đã bật báo động SMS/App",
    "actions": [
      [
        "📣",
        "Đăng tin tìm mua gấp (Bounty)",
        "primary full"
      ]
    ]
  },
  {
    "id": "item-5",
    "category": "rare boxset",
    "badge": "Dark Horse Hardcover",
    "badgeClass": "dark",
    "condition": "",
    "image": "https://lh3.googleusercontent.com/aida-public/AB6AXuB4ktowsBeMpD6PTKU7CrtzchU0ngZhny3Ba-Oqz91Dc5CbnTMdltsQ0Y9E8XYcpH6HmRf9MW2XQ84NQeyGA6smzI1C3gQonzAGqJWqkYJke-4-5VAZfEmsI1WQPAk_UeKyRhBovDATXhInbUni6aFOSn7ElhrqsjtVXIJcz7Xtgo7Ml-uqE_H4L6SnaIkHhpey-GjjZtV6ykwxmRwpeJpiFUbXtSimsZK0PDytcOjdONaVDxEduPsPpg",
    "status": "Có 2 bộ sang nhượng",
    "place": "Bìa da thật",
    "meta": "Tập 1 - 5 Deluxe • Kentarou Miura",
    "title": "Berserk Deluxe Edition Vol 1-5",
    "sub": "Sách nhập khẩu Anh/Mỹ chính hãng",
    "price": "2.850.000đ",
    "old": "3.400.000đ",
    "actions": [
      [
        "🛍",
        "Mua ngay",
        "primary"
      ],
      [
        "💬",
        "Thương lượng",
        "secondary"
      ]
    ]
  },
  {
    "id": "item-6",
    "category": "deal boxset",
    "badge": "🏷 Giảm 20%",
    "badgeClass": "deal",
    "condition": "Độ mới: 95%",
    "image": "https://lh3.googleusercontent.com/aida-public/AB6AXuByUY0d91VqamRukxuv--zo74lBg9ffZmV2w1Z8ULzQnoCLHEak6JwMqcbzTqGM03LSDXEdIcJE2VRG6P3lQYvAjwRNbARI-QXgwhXUK94dPGkL-AkhfOQ_XryPlJ4y3DdW7FLUQMjz1DW6HmrJxbFDqZg5PmDq6D3Cn60PZPfUonNGmFDfMwpJujqeTCLS6TDdfvUz_owmccKnh-sgw6-zp0pP5j6ewRL3UtmkG38eBljvYiZPWDHaZA",
    "status": "Đang có 4 người đăng bán",
    "place": "Đà Nẵng",
    "meta": "Tập 1 - 100 Trọn bộ • Eiichiro Oda",
    "title": "One Piece (Bộ 1 - 100 Bìa Rời)",
    "sub": "NXB Kim Đồng - Tặng kèm 4 poster truy nã",
    "price": "1.950.000đ",
    "old": "2.450.000đ",
    "actions": [
      [
        "🛒",
        "Mua ngay",
        "primary"
      ],
      [
        "🏪",
        "Xem giá rẻ nhất",
        "secondary"
      ]
    ]
  },
  {
    "id": "item-7",
    "category": "deal",
    "badge": "🔥 Giảm 10%",
    "badgeClass": "deal",
    "condition": "",
    "image": "https://lh3.googleusercontent.com/aida-public/AB6AXuAXJLCXGod2kloWMl8qCwpk0YN4iiwZ87gvLBpjhu5dgknbvox2_cr65ghwkz-Kn-MzY_QsMKBlSgPNYgeCFZli1biEaaSvD-GFsrkVpMsCYDuBk7_vMY8XqwmvU7CzBCIyh_94rO3_6lhxBy9LxForvdi-8d4AQWUTBfCht7xAYIBAYWyVfyk28mSD8JwPZBWuvHWCiskvbLp3aujgVcBg_7I0r_RzDmARQGclME0SGmtFl4w9dVCFHw",
    "status": "1 người bán duy nhất",
    "place": "Cần Thơ",
    "meta": "Trọn bộ 24 tập Deluxe • Kim Đồng",
    "title": "Slam Dunk Deluxe (Full 24 Tập)",
    "sub": "Bản bìa màng ép đặc biệt, không ố vàng",
    "price": "1.400.000đ",
    "old": "1.560.000đ",
    "actions": [
      [
        "🛒",
        "Mua ngay",
        "primary"
      ],
      [
        "💬",
        "Nhắn tin",
        "secondary"
      ]
    ]
  },
  {
    "id": "item-8",
    "category": "rare deal boxset",
    "badge": "★ Bản dịch NXB Trẻ 2014",
    "badgeClass": "rare",
    "condition": "",
    "image": "https://lh3.googleusercontent.com/aida-public/AB6AXuAUyPw-BBhNhzg301DtS-KlI9oVqCWpuUrERdTX4wN3lORh2Rl3u3C5zDzdTRodGGbWI5I56RBsOPumVaR_TAA_XjDfdzSRb5xoDqA3gr49j7la3HVBV1r_gROO9hFeR9NUMlptxrtt4fXd7wLFSamVCgy-R_-rC7fcwTDFziZ9vavX0dngycGd8VdRcOrnUkUqG5fYeTUz2VJmajqEDJUy2pfrkkawlQ-qefqgPd7rodv3fJfE9rWK8g",
    "status": "2 người đang sang nhượng",
    "place": "Bảo quản bọc kiếng",
    "meta": "Full 22 Tập + 2 Tập 21st • NXB Trẻ",
    "title": "20th Century Boys (Full 24 Cuốn)",
    "sub": "Tình trạng: 93% không rách rời",
    "price": "1.150.000đ",
    "old": "1.350.000đ",
    "actions": [
      [
        "🛒",
        "Mua ngay",
        "primary"
      ],
      [
        "🏪",
        "Xem người bán",
        "secondary"
      ]
    ]
  }
];

/* ============================================================
   MANGATRADE — TRUYỆN YÊU THÍCH
   Vanilla JavaScript
============================================================ */

document.addEventListener("DOMContentLoaded", () => {
  // ---------- DOM ----------
  const cardsContainer = document.getElementById("wishlistCardsContainer");
  const searchInput = document.getElementById("wishlistSearchInput");
  const filterTabs = document.querySelectorAll(".wishlist-tab");
  const noResults = document.getElementById("noResultsNotice");

  const bulkBar = document.getElementById("bulkActionBar");
  const selectedBadge = document.getElementById("selectedCountBadge");
  const selectAllButton = document.getElementById("toggleSelectAllBtn");
  const selectAllText = document.getElementById("selectAllText");

  const modal = document.getElementById("addMangaModal");
  const addForm = document.getElementById("addWishlistForm");

  const toast = document.getElementById("wishlistToast");
  const toastMessage = document.getElementById("toastMessage");
  const toastIcon = document.getElementById("toastIcon");

  let currentFilter = "all";
  let toastTimer = null;

  // ---------- Toast ----------
  function showToast(message, icon = "check_circle") {
    toastMessage.textContent = message;
    toastIcon.textContent = icon;

    toast.classList.add("show");

    clearTimeout(toastTimer);
    toastTimer = setTimeout(() => {
      toast.classList.remove("show");
    }, 3000);
  }

  // ---------- Card HTML ----------
  function createCard(item) {
    const actionButtons = item.actions.map(([icon, label, type]) => {
      const fullClass = type.includes("full") ? "full" : "";
      const baseType = type.split(" ")[0];

      return `
        <button class="btn btn-${baseType} ${fullClass}" type="button">
          <span>${icon}</span>
          ${label}
        </button>
      `;
    }).join("");

    return `
      <article class="wishlist-card" data-id="${item.id}" data-category="${item.category}">
        <div class="card-media">
          <img
            src="${item.image}"
            alt="${item.title}"
            loading="lazy"
          >

          <div class="card-badges">
            <span class="badge ${item.badgeClass}">
              ${item.badge}
            </span>

            ${item.condition ? `<span class="condition-badge">${item.condition}</span>` : ""}
          </div>

          <label class="item-checkbox-wrapper" title="Chọn tác phẩm">
            <input class="item-checkbox" type="checkbox">
          </label>

          <button
            class="wishlist-heart-btn"
            type="button"
            title="Bỏ khỏi yêu thích"
            aria-label="Bỏ khỏi yêu thích"
          >
            <span class="material-symbols-outlined">favorite</span>
          </button>

          <div class="media-bottom">
            <div class="media-bottom-inner">
              <span class="hot">${item.status}</span>
              <span class="place">${item.place}</span>
            </div>
          </div>
        </div>

        <div class="card-body">
          <div>
            <div class="card-meta">${item.meta}</div>

            <h3 class="card-title" title="${item.title}">
              ${item.title}
            </h3>

            <p class="card-subtitle" title="${item.sub}">
              ${item.sub}
            </p>

            ${item.price ? `
              <div class="card-price">
                <strong>${item.price}</strong>
                ${item.old ? `<span class="old-price">${item.old}</span>` : ""}
                ${item.note ? `<span class="note">${item.note}</span>` : ""}
              </div>
            ` : ""}

            ${item.note && !item.price ? `<div class="card-note">${item.note}</div>` : ""}
          </div>

          <div>
            <div class="card-actions">
              ${actionButtons}
            </div>

            <button class="card-delete" type="button">
              🗑 Xóa khỏi danh sách
            </button>
          </div>
        </div>
      </article>
    `;
  }

  function renderCards() {
    cardsContainer.innerHTML = WISHLIST_ITEMS.map(createCard).join("");
    bindCardEvents();
    applyFilters();
  }

  // ---------- Filtering ----------
  function applyFilters() {
    const query = searchInput.value.trim().toLowerCase();
    let visibleCount = 0;

    document.querySelectorAll(".wishlist-card").forEach(card => {
      const content = card.innerText.toLowerCase();
      const categories = card.dataset.category || "";

      const matchesSearch = content.includes(query);
      const matchesFilter =
        currentFilter === "all" ||
        categories.split(" ").includes(currentFilter);

      const visible = matchesSearch && matchesFilter;

      card.style.display = visible ? "flex" : "none";

      if (visible) {
        visibleCount++;
      }
    });

    noResults.hidden = visibleCount !== 0;
  }

  filterTabs.forEach(tab => {
    tab.addEventListener("click", () => {
      filterTabs.forEach(item => item.classList.remove("active"));

      tab.classList.add("active");
      currentFilter = tab.dataset.filter;

      applyFilters();
    });
  });

  searchInput.addEventListener("input", applyFilters);

  // ---------- Card events ----------
  function bindCardEvents() {
    document.querySelectorAll(".item-checkbox").forEach(checkbox => {
      checkbox.addEventListener("change", updateBulkSelection);
    });

    document.querySelectorAll(".wishlist-heart-btn").forEach(button => {
      button.addEventListener("click", event => {
        event.stopPropagation();

        const icon = button.querySelector(".material-symbols-outlined");
        const isOff = button.classList.toggle("is-off");

        icon.style.fontVariationSettings = isOff
          ? "'FILL' 0"
          : "'FILL' 1";

        showToast(
          isOff
            ? "Đã tạm ngừng theo dõi truyện này."
            : "Đã thêm lại vào danh sách radar săn truyện!",
          isOff ? "favorite_border" : "favorite"
        );
      });
    });

    document.querySelectorAll(".card-delete").forEach(button => {
      button.addEventListener("click", () => {
        const card = button.closest(".wishlist-card");

        if (!card) return;

        card.style.opacity = "0";
        card.style.transform = "scale(.96)";

        setTimeout(() => {
          card.remove();
          updateBulkSelection();
          applyFilters();
          showToast("Đã xóa 1 tác phẩm khỏi danh sách yêu thích.", "delete");
        }, 200);
      });
    });

    document.querySelectorAll(".card-actions .btn").forEach(button => {
      button.addEventListener("click", () => {
        showToast(`Đã chọn: ${button.textContent.trim()}`);
      });
    });
  }

  // ---------- Bulk selection ----------
  function getSelectedCheckboxes() {
    return [...document.querySelectorAll(".item-checkbox:checked")];
  }

  function updateBulkSelection() {
    const selected = getSelectedCheckboxes();
    const count = selected.length;

    selectedBadge.textContent = count;

    if (count > 0) {
      bulkBar.classList.add("show");
      bulkBar.setAttribute("aria-hidden", "false");
      selectAllText.textContent = `Đã chọn (${count})`;
    } else {
      bulkBar.classList.remove("show");
      bulkBar.setAttribute("aria-hidden", "true");
      selectAllText.textContent = "Chọn nhiều";
    }
  }

  selectAllButton.addEventListener("click", () => {
    const visibleCheckboxes = [...document.querySelectorAll(".wishlist-card")]
      .filter(card => card.style.display !== "none")
      .map(card => card.querySelector(".item-checkbox"))
      .filter(Boolean);

    if (!visibleCheckboxes.length) {
      showToast("Không có tác phẩm nào để chọn.", "info");
      return;
    }

    const shouldSelect = !visibleCheckboxes.every(box => box.checked);

    visibleCheckboxes.forEach(box => {
      box.checked = shouldSelect;
    });

    updateBulkSelection();
  });

  document.getElementById("bulkCancelBtn").addEventListener("click", () => {
    document.querySelectorAll(".item-checkbox").forEach(box => {
      box.checked = false;
    });

    updateBulkSelection();
  });

  document.getElementById("bulkDeleteBtn").addEventListener("click", () => {
    const selected = getSelectedCheckboxes();

    if (!selected.length) return;

    selected.forEach(box => {
      const card = box.closest(".wishlist-card");
      card?.remove();
    });

    updateBulkSelection();
    applyFilters();
    showToast(`Đã xóa ${selected.length} tác phẩm khỏi danh sách.`, "delete");
  });

  document.getElementById("bulkCreateBountyBtn").addEventListener("click", () => {
    const count = getSelectedCheckboxes().length;

    if (!count) return;

    showToast(
      `Đã gom ${count} truyện để lập bài đăng tìm mua trọn gói!`,
      "campaign"
    );

    document.querySelectorAll(".item-checkbox").forEach(box => {
      box.checked = false;
    });

    updateBulkSelection();
  });

  document.getElementById("bulkAddToCartBtn").addEventListener("click", () => {
    const count = getSelectedCheckboxes().length;

    if (!count) return;

    showToast(`Đã chuyển ${count} truyện vào giỏ hàng!`, "shopping_cart");

    document.querySelectorAll(".item-checkbox").forEach(box => {
      box.checked = false;
    });

    updateBulkSelection();
  });

  // ---------- Modal ----------
  function openModal() {
    modal.classList.add("show");
    modal.setAttribute("aria-hidden", "false");

    const firstInput = modal.querySelector("input");
    firstInput?.focus();
  }

  function closeModal() {
    modal.classList.remove("show");
    modal.setAttribute("aria-hidden", "true");
  }

  document.getElementById("openAddModalBtn").addEventListener("click", openModal);
  document.getElementById("closeAddModalBtn").addEventListener("click", closeModal);
  document.getElementById("cancelAddModalBtn").addEventListener("click", closeModal);

  if (window.location.hash === "#openAddModalBtn") {
    openModal();
  }

  modal.addEventListener("click", event => {
    if (event.target === modal) {
      closeModal();
    }
  });

  addForm.addEventListener("submit", event => {
    event.preventDefault();

    closeModal();
    addForm.reset();

    showToast(
      "Đã thêm truyện vào radar theo dõi yêu thích!",
      "radar"
    );
  });

  // ---------- Radar banner ----------
  document.getElementById("viewDealsBtn").addEventListener("click", () => {
    currentFilter = "deal";

    filterTabs.forEach(tab => {
      tab.classList.toggle("active", tab.dataset.filter === "deal");
    });

    applyFilters();
    showToast("Đã lọc các truyện đang có deal giảm giá.", "local_fire_department");
  });

  document.getElementById("closeRadarBtn").addEventListener("click", () => {
    document.getElementById("radarBanner").remove();
  });

  document.getElementById("createMultiBountyBtn").addEventListener("click", () => {
    showToast(
      "Đang quét 14 truyện yêu thích để khởi tạo form tìm mua...",
      "campaign"
    );
  });

  // ---------- Global search ----------
  const globalSearch = document.getElementById("globalSearchInput");

  globalSearch.addEventListener("keydown", event => {
    if (event.key === "Enter" && globalSearch.value.trim()) {
      searchInput.value = globalSearch.value.trim();
      currentFilter = "all";

      filterTabs.forEach(tab => {
        tab.classList.toggle("active", tab.dataset.filter === "all");
      });

      applyFilters();
      showToast(`Đang tìm kiếm: "${globalSearch.value.trim()}"`, "search");
    }
  });

  document.addEventListener("keydown", event => {
    if ((event.ctrlKey || event.metaKey) && event.key.toLowerCase() === "k") {
      event.preventDefault();
      globalSearch.focus();
    }

    if (event.key === "Escape" && modal.classList.contains("show")) {
      closeModal();
    }
  });

  // ---------- Sort ----------
  document.getElementById("sortSelect").addEventListener("change", event => {
    const value = event.target.value;

    if (value === "price") {
      showToast("Đã áp dụng sắp xếp theo giá bán tốt nhất.", "sort");
    } else if (value === "rare") {
      showToast("Đã áp dụng sắp xếp theo mức độ khan hiếm.", "diamond");
    } else if (value === "condition") {
      showToast("Đã áp dụng sắp xếp theo độ mới.", "stars");
    } else {
      showToast("Đã áp dụng: Mới thêm gần đây.", "schedule");
    }
  });

  // ---------- Newsletter ----------
  document.getElementById("newsletterForm").addEventListener("submit", event => {
    event.preventDefault();

    const email = document.getElementById("newsletterEmail");

    if (!email.checkValidity()) {
      showToast("Vui lòng nhập email hợp lệ.", "error");
      return;
    }

    showToast("Đăng ký bản tin thành công!", "mark_email_read");
    email.value = "";
  });

  // ---------- Start ----------
  renderCards();
});
